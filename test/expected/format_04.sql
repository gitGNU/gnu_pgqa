SELECT product_id, SUM(nitems) AS
sum_nitems, SUM(price) AS sum_price FROM
sales GROUP BY product_id, 3 ORDER BY
product_id, 3

SELECT          product_id, SUM(nitems) AS sum_nitems, SUM(price) AS
                sum_price
FROM            sales
GROUP BY        product_id, 3
ORDER BY        product_id, 3

SELECT          product_id, SUM(nitems) AS sum_nitems, SUM(price) AS
                sum_price
FROM            sales
GROUP BY        product_id, 3
ORDER BY        product_id, 3

SELECT          product_id, SUM(nitems) AS sum_nitems, SUM(price) AS
                sum_price
FROM            sales
GROUP BY        product_id, 3
ORDER BY        product_id, 3

SELECT          product_id, SUM(nitems) AS sum_nitems, SUM(price) AS sum_price
FROM            sales
GROUP BY        product_id, 3
ORDER BY        product_id, 3

SELECT
        product_id, SUM(nitems) AS sum_nitems, SUM(price) AS sum_price
FROM
        sales
GROUP BY
        product_id, 3
ORDER BY
        product_id, 3

SELECT
        product_id, SUM(nitems) AS sum_nitems, SUM(price) AS sum_price
FROM
        sales
GROUP BY
        product_id, 3
ORDER BY
        product_id, 3

SELECT
    product_id, SUM(nitems) AS sum_nitems, SUM(price) AS sum_price
FROM
    sales
GROUP BY
    product_id, 3
ORDER BY
    product_id, 3

SELECT          product_id,
                SUM(nitems)
                AS
                sum_nitems,
                SUM(price)
                AS
                sum_price
FROM            sales
GROUP BY        product_id,
                3
ORDER BY        product_id,
                3

SELECT
        product_id,
        SUM(nitems) AS sum_nitems,
        SUM(price) AS sum_price
FROM
        sales
GROUP BY
        product_id,
        3
ORDER BY
        product_id,
        3

SELECT
        product_id,
        SUM(nitems) AS sum_nitems,
        SUM(price) AS sum_price
FROM
        sales
GROUP BY
        product_id,
        3
ORDER BY
        product_id,
        3

                SELECT product_id, SUM(nitems) AS sum_nitems, SUM(price) AS
                sum_price FROM sales GROUP BY product_id, 3 ORDER BY
                product_id, 3

                SELECT          product_id, SUM(nitems) AS sum_nitems, SUM(price)
                                AS sum_price
                FROM            sales
                GROUP BY        product_id, 3
                ORDER BY        product_id, 3

                SELECT
                        product_id, SUM(nitems) AS sum_nitems, SUM(price) AS
                        sum_price
                FROM
                        sales
                GROUP BY
                        product_id, 3
                ORDER BY
                        product_id, 3